{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch Foundry",
			"type": "chrome",
			"request": "launch",
			"url": "https://localhost:30000/game",
			"webRoot": "${workspaceFolder}",
			"cwd": "${workspaceFolder}",
			"pathMapping": {
				"modules": "${workspaceFolder}"
			}
		}
	]
}